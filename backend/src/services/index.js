const auth = require("./auth.services");
const post = require("./post.services");
const roles = require("./roles.services");
const user = require("./user.services");
const tag = require("./tag.services");

module.exports = { auth, post, roles, user, tag };
